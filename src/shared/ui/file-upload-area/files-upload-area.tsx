export interface FilesUploadAreaProps {
  onFilesChange: (files: File[]) => void;
}

export const FilesUploadArea = ({ onFilesChange }: FilesUploadAreaProps) => {
  return <div>FilesUploadArea</div>;
};
